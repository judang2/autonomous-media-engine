# 로컬 검증 결과

검증일: 2026-09-08. Windows, Python 3.12 런타임에서 확인했습니다.

- 표준 라이브러리만으로 `python -m igagent` 실행: 샘플 DM 답장 `dry_run` 출력 성공.
- 패키지 빌드 및 작업용 가상환경 설치 성공.
- 선택적 `instagrapi==2.18.18` 설치, 실제 라이브러리의 세션 필드와 호출 시그니처 확인.
- `python -m unittest discover -s tests -v`: **21개 통과**. private 의존성 없이 실행하면 18개 통과 + 3개 생략합니다.
- 네트워크 없는 dry-run, 정책 차단, 23시간 응답 창, 잘못된 모델 출력, 모델 거절, 영속 중복 방지, 최근 24시간 한도, 간격, 불확실 전송, STOP, 로그 본문 제외 검증.
- Graph 댓글 정규화·DM 전송 경로·컨테이너 준비 후 게시를 모의 응답으로 확인.
- private 실제 라이브러리 클라이언트에서 타임아웃·challenge가 재시도/자동 처리되지 않는지 대역 전송 함수로 확인.
- private 사진 configure 단일 호출 및 `media/` 밖의 경로 차단 확인.

실제 Instagram 로그인, Meta 권한 승인, 토큰 교환, 실제 GPT 생성, DM·댓글 전송, 좋아요·사진 게시의 성공 여부는 **미검증**입니다. 사용자의 자격 증명은 요청하거나 읽지 않았으며 실계정에 접근하지 않았습니다.

네트워크 설치는 공개 Python 의존성 다운로드에만 사용했습니다. mock 테스트의 키와 ID는 가짜 값입니다. 샘플 출력은 `examples/demo-result.json`에서 볼 수 있습니다.
